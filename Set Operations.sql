USE DUMMYDB;
SELECT *
FROM EMPLOYEES
WHERE SALARY > 10000
UNION
SELECT *
FROM EMPLOYEES
WHERE DEPARTMENT_ID=100;
SELECT *
FROM EMPLOYEES
WHERE SALARY > 10000
UNION ALL
SELECT *
FROM EMPLOYEES
WHERE DEPARTMENT_ID=100;

SELECT *
FROM EMPLOYEES
WHERE SALARY > 10000
INTERSECT
SELECT *
FROM EMPLOYEES
WHERE DEPARTMENT_ID=100;

SELECT *
FROM EMPLOYEES
WHERE SALARY > 10000
EXCEPT #MINUS 
SELECT *
FROM EMPLOYEES
WHERE DEPARTMENT_ID=100

